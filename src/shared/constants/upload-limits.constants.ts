/** Max multipart file size (bytes) — Multer + FileValidationInterceptor. */
export const MAX_UPLOAD_FILE_BYTES = 20 * 1024 * 1024;

export const MAX_UPLOAD_FILE_MB = MAX_UPLOAD_FILE_BYTES / (1024 * 1024);
