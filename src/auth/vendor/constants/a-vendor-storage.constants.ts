/**
 * Storage folder for A-Vendor user assets (S3 key prefix / Cloudinary folder).
 * Results in paths like: a-vendor/user/display-pic-x7k2m.jpg
 */
export const A_VENDOR_USER_STORAGE_FOLDER = 'a-vendor/user';

/** Object basename prefix (random segment appended by StorageService). */
export const A_VENDOR_DISPLAY_PIC_BASENAME_PREFIX = 'display-pic';
